; (executable
;  (public_name opeg_boot)
;  (package opeg_boot)
;  (name main)
;  (libraries grammar utils parsetree opegLib))

; (ocamllex lexer)

; (menhir 
;  (modules boot_parser)
;  (flags --explain))
