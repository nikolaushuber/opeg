token:
    | TOK_A "a"
    | TOK_B "b" 
    | TOK_C "c" 
    | TOK_EOF "eof"

parser "parse" start

%% 

start <bool>:
    / "a"? rule1+ "c" "eof" { true }
    / "c"* "eof" { true }

rule1 <bool>:
    / "b" { true }

